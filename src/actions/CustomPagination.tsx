import { Pagination } from "@mantine/core";

const CustomPagination = () => {
  return (
    <Pagination className="mt-32 !gap-2 justify-center relative" total={10} />
  );
};

export default CustomPagination;
